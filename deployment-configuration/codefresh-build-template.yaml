type: build
stage: build
dockerfile: Dockerfile
registry: '${{CODEFRESH_REGISTRY}}'
buildkit: true
build_arguments:
  - DOMAIN=${{DOMAIN}}
  - NOCACHE=${{CF_BUILD_ID}}
  - NEUROGLANCER_BRANCH_NAME=${{NEUROGLANCER_BRANCH_NAME}}
  - FORCE_CLONE=${{FORCE_CLONE}}
